#ifndef PRELUDE_H
#define PRELUDE_H

#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>

inline int max(int x, int y) { return x > y ? x : y; }

inline int min(int x, int y) { return x < y ? x : y; }

#endif
